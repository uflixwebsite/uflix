name: Keep Uflix Backend Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Uflix Render health endpoint
        run: |
          curl -fs https://uflix.onrender.com/health > /dev/null
